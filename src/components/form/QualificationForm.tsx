// DEPRECATED — el formulario ahora vive en QualificationModal.tsx (modal global manejado por ModalProvider).
// Este archivo se mantiene como stub para no romper imports legacy.

'use client';

export { QualificationModal as QualificationForm } from './QualificationModal';
